doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Elice
        //- link(rel="stylesheet", href="https://unpkg.com/mvp.css")
        link(rel="stylesheet", href="/public/stylesheets/style.css")
    body 
        header 
            h1 Elice
        main
            div#welcome
                form#name
                    input(placeholder="Nickname", required, type="text")
                    button Enter
                form#roomname
                    input(placeholder="room name", required, type="text")
                    button Enter
                div#roomList
                    h4 Room List
                    ul
            div#call
                div#myStream
                    div#my
                        video#myFace(autoplay, playsinline, width="300", height="300")

                    div#peer
                        video#peerFace(autoplay, playsinline, width="300", height="300")
                    div#chat
                        h3 Chat
                        div#chatbox
                        form#msg
                            input(placeholder="Typing Message", required, type="text")
                            button Send
                    div#buttons
                        button#mute Mic
                        button#camera Cam
                    select#cameras
                    textarea#textarea(type="text", cols="70", rows="20", spellcheck="false") 
                    p(style="margin-left:50px;") ※한글이 아닌 영어, 숫자, 특수문자의 경우 마지막에 한글을 입력해야 상대방의 화면에도 적용됩니다.
                    div.container
                        div.message 오목
                        div.buttons
                            button#reload Reload
                        div.winShow1 검정색 승리!
                        div.winShow2 하얀색 승리!
                        canvas#canvas
                         
        script(src="/socket.io/socket.io.js")
        script(src="/public/js/app.js")